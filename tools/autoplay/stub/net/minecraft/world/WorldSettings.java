package net.minecraft.world;

/**
 * Autoplay compile stub: descriptor-only 1.12.2 WorldSettings (the
 * companion passes null, the type only names the launchIntegratedServer
 * slot). Class names are identical in MCP and SRG, so no mapping is ever
 * needed — same practice as bridge-1165.
 */
public class WorldSettings {
}
