package net.minecraftforge.fml.common.eventhandler;

/** C1 compile stub, never runs (see Mod.java). Loot companion posts
 * simulated harvest/kill events through this bus (measured
 * {@code post(Event)} on the pinned 2860 universal). */
public class EventBus {
    public void register(Object target) {
    }

    public boolean post(Event event) {
        return false;
    }
}
