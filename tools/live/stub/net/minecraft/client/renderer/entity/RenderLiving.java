package net.minecraft.client.renderer.entity;

import net.minecraft.entity.EntityLivingBase;

/**
 * Vanilla compile stub: shape-only 1.12.2 client class used by
 * {@code forge/} sources. Never runs. Hierarchy mirrors the runtime so
 * the beast renderer method reference resolves through the real chain.
 */
public class RenderLiving<T extends EntityLivingBase> extends Render<T> {
    protected RenderLiving(RenderManager manager) {
        super(manager);
    }
}
