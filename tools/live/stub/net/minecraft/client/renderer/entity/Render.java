package net.minecraft.client.renderer.entity;

import net.minecraft.entity.Entity;

/**
 * Vanilla compile stub: shape-only 1.12.2 client class used by
 * {@code forge/} sources. Never runs. Hierarchy mirrors the runtime
 * (measured: notch {@code cak extends caf<aad>} from the pinned client
 * jar) — the erased descriptor is what links, never the MCP names.
 */
public class Render<T extends Entity> {
    protected Render(RenderManager manager) {
    }
}
