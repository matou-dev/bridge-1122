package net.minecraft.util.math;

/**
 * Loot compile stub: declaring type of getX/getY/getZ on 2860 (BlockPos
 * inherits them — forge reads coords through this type, never through
 * BlockPos, so Reobf maps the exact bytecode owner). Never runs.
 */
public class Vec3i {
    public int getX() {
        return 0;
    }

    public int getY() {
        return 0;
    }

    public int getZ() {
        return 0;
    }
}
