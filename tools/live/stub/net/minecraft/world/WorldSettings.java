package net.minecraft.world;

/**
 * Shape-only compile stub: descriptor-only 1.12.2 WorldSettings (the
 * companion passes null, the type only names the launchIntegratedServer
 * slot). Lives with the other vanilla shapes under tools/live/stub —
 * the merged tools/live/stub Minecraft stub names it, so it must be on
 * the forge compile classpath too. Class names are identical in MCP and
 * SRG, so no mapping is ever needed — same practice as bridge-1165.
 */
public class WorldSettings {
}
