package net.minecraft.nbt;

/**
 * Spawn-identity compile stub: the 2860 persist surface the bridge beast
 * extends with its short mob name ({@code MatouEntity} writes/reads one
 * string tag — owner discipline, hub decisions/LOOT.md). Never runs
 * (compile classpath only).
 */
public class NBTTagCompound {
    public boolean hasKey(String key) {
        return false;
    }

    public String getString(String key) {
        return "";
    }

    public void setString(String key, String value) {
    }
}
